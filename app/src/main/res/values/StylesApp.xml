<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Configuracion estilos de la interfaz de la aplicacion -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="colorPrimary">@color/appColorPrimary</item>
        <item name="colorPrimaryDark">@color/appColorPrimaryDark</item>
        <item name="colorAccent">@color/appColorAccent</item>
    </style>

    <style name="CustomActionBar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>

    <style name="Widget.CardContent" parent="android:Widget">
        <item name="android:paddingLeft">@dimen/appPaddingLeft</item>
        <item name="android:paddingRight">@dimen/appPaddingRight</item>
        <item name="android:paddingTop">@dimen/appPaddingTop</item>
        <item name="android:paddingBottom">@dimen/appPaddingBottom</item>
        <item name="android:orientation">vertical</item>
    </style>

</resources>
